<view class="container">
  <view class="gradientview"></view>
  <view class="container-contain"></view>
  <base-panel>
    <simple-goods-card cart-list="{{cartList}}" produce="{{item.produce}}" wx:key="item.produce.selectedGoods.id"
      wx:for="{{cartList}}"/>
  </base-panel>
</view>